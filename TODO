1. FIX 0 offset bobber (reset bobber check when offset is 0)
2. FEAT add additional hsv regions for bobber checking (it might be non full red color (night color etc.)) (so little pixels count)
3. FEAT selectable region